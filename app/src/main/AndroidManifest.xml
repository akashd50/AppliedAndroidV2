<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akash.appliedandroidv2">
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity"
            android:label= "SecondScreen"
            android:theme="@style/AppTheme"/>

        <activity android:name=".CanvasActivity"
            android:label = "CanvasClass"
            android:theme="@style/AppTheme"/>

        <activity android:name=".ChessGameActivity"
            android:label="ChessGameActivity"
            android:theme="@style/AppTheme"/>

        <activity android:name=".GameLoop"
            android:label = "GameLoop"
            android:theme="@style/AppTheme"/>

        <activity android:name=".GLRendererActivity"
            android:label="GlRendererActivity"
           />


    </application>

</manifest>